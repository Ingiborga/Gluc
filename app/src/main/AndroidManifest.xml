<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:label="Test GLU"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">  <!-- ← ГЛАВНОЕ: ДОБАВЬТЕ ЭТУ СТРОКУ -->
        <activity
            android:name="com.test.MainActivity"
            android:exported="true" />
        <activity
            android:name="com.test.ProfileActivity"
            android:exported="false" />
        <activity
            android:name="com.test.AuthActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.test.GraphActivity"
            android:exported="false" />

        <service
            android:name="com.test.broadcast.BroadcastService"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="com.test.broadcast.AidexBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.librelink.app.ThirdPartyIntegration.GLUCOSE_READING" />
            </intent-filter>
        </receiver>
    </application>
</manifest>